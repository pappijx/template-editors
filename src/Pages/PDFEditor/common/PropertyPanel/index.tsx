import { PropertyPanelContainer } from "./style";

const PropertyPanel = ({ children }: any) => {
  return <PropertyPanelContainer>{children}</PropertyPanelContainer>;
};

export default PropertyPanel;
